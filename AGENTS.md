# Repository Working Rules

このファイルは、このリポジトリを扱うすべてのCodexセッションに適用する。

## ドキュメント更新は実装完了条件

新機能、仕様変更、配置変更、モデル変更、データ構造変更、モジュール追加を実装するときは、実装と同じブランチ・同じPull Requestで`docs/`配下の関連文書を更新すること。コードだけを変更して完了としてはならない。

作業開始時に[`docs/README.md`](docs/README.md)と関連仕様書を読み、正本と更新先を確認する。

- 敷地、建物、地表、植栽、設備、座標、寸法、見た目、REAL / PLANの挙動を変更した場合: `docs/MODEL-SPEC.md`
- 固定データ、設計差分、保存形式、検証、変更ルールを変更した場合: `docs/DATA-GOVERNANCE.md`
- ファイル構成、読み込み順、状態管理、編集機能、リソース管理、キャッシュ構造を変更した場合: `docs/ARCHITECTURE.md`
- 文書構成や正本の場所を変更した場合: `docs/README.md`

複数分類に影響する実装では、該当する文書をすべて更新する。

数値配列をMarkdownへ重複コピーせず、機械可読な正本への参照、値の意味、代表値、根拠、確定／暫定の区別を記録する。一時的な作業指示、ブランチ名、コミット手順だけの仕様書を新設せず、恒久情報を既存の分類へ統合する。

ドキュメント変更が本当に不要な修正では、Pull Request本文に「Docs impact: なし」と理由を記載する。実装完了時には、関連文書を更新したか、その理由を明記したことを確認する。

## 固定データ

`data/fixed-site-data.js`を表示調整や実行時状態のために変更しない。固定値を変更する場合は`docs/DATA-GOVERNANCE.md`に従い、根拠、変更前後の差分、検証結果を記録する。

## 最低限の検証

変更範囲に応じて、少なくとも次を確認する。

- 変更したJavaScriptの`node --check`
- `git diff --check`
- 関連する`scripts/`配下のテスト
- 文書内のリンクと旧ファイル名への参照
- 表示変更ではREAL / PLANおよび対象ワークスペース
